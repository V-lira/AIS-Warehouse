<Window x:Class="AIS.Warehouse.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIS.Warehouse.UI.Views"
        mc:Ignorable="d"
        Title="Вход в систему" Height="300" Width="400"  WindowStartupLocation="CenterScreen" Background="Beige">
    <Grid Margin="20">
        <StackPanel Background="Beige">
            <TextBlock Text="Вход" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" FontFamily="Bodoni MT Black"/>
            <TextBox Margin="0,5" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" FontFamily="Bodoni MT Black"/>
            <PasswordBox Margin="0,5" PasswordChanged="PasswordBox_PasswordChanged" FontFamily="Bodoni MT Black"/>
            <Button Content="Войти" Height="40" Margin="0,20,0,0" Command="{Binding LoginCommand}" Background="#FFD0D09E" FontFamily="Bodoni MT Black" />
        </StackPanel>
    </Grid>
</Window>