<UserControl x:Class="AIS.Warehouse.UI.Views.ItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="#FFD0D09E" Padding="10">
                <TextBlock Text="Список товаров" FontSize="20" FontWeight="Bold" Foreground="White" FontFamily="Bodoni MT Black"/>
            </Border>
            <ListView ItemsSource="{Binding Items}" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                        <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Name}" Width="200"/>
                        <GridViewColumn Header="Артикул" DisplayMemberBinding="{Binding ItemCode}" Width="120"/>
                        <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding Category}" Width="150"/>
                        <GridViewColumn Header="Количество" DisplayMemberBinding="{Binding Quantity}" Width="80"/>
                        <GridViewColumn Header="Ед. измерения" DisplayMemberBinding="{Binding Unit}" Width="100"/>
                        <GridViewColumn Header="Цена" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:C}}"
                                               HorizontalAlignment="Right"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Grid>
</UserControl>