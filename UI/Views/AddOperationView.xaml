<UserControl x:Class="AIS.Warehouse.UI.Views.AddOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Padding="20">
        <StackPanel Width="350">
            <TextBlock Text="Добавление операции" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#FFB1B163" FontFamily="Bodoni MT Black"/>
            <TextBlock Text="Тип операции" Foreground="#FFB1B163"/>
            <ComboBox ItemsSource="{Binding OperationTypes}" SelectedItem="{Binding SelectedOperationType}" Margin="0,5,0,15"/>
            <TextBlock Text="Товар" Foreground="#FFB1B163" FontFamily="Bodoni MT Black"/>
            <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" DisplayMemberPath="Name" Margin="0,5,0,15"/>
            <TextBlock Text="Количество" Foreground="#FFB1B163" FontFamily="Bodoni MT Black"/>
            <TextBox Text="{Binding Quantity}" Margin="0,5,0,15"/>
            <Button Content="Сохранить операцию" Height="40" Command="{Binding SaveCommand}" Background="#FFD0D09E" FontFamily="Bodoni MT Black" Foreground="#FFB1B163"/>
        </StackPanel>
    </Border>
</UserControl>
